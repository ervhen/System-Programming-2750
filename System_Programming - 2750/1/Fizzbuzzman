.TH FizzBuzz 1 "It does a thing"
.SH NAME
 FizzBuzz - Displays FizzBuzz if number is divisible by 3 and 5, Fizz if divisible by 5, Buzz if divisible by 3.
.SH SYNOPSIS
.B FizzBuzz [n]

.SH DISCRIPTION
FizzBuzz counts down from [n] to 1 and displays FizzBuzz if the number is divisble by 5 and 3. It displays Fizz if [n] is divisible by 5, and Buzz if [n] is divisible by 3. If [n] is not divisible by 5 or 3 then [n] is displayed. FizzBuzz is for an assignment and it isn't very usefull. It does work though! 
.PP
FizzBuzz can be used without passing a number, if that is the case then 20 is used.

.SH SEE ALSO
There isn't much to see here.
